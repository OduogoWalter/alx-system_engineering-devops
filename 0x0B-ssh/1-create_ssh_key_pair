#!/usr/bin/env bash
# This script creates an RSA key pair

# Generate an RSA key pair with 4096 bits and protect it with passphrase "betty"
ssh-keygen -t rsa -b 4096 -C "Generated by script" -N "betty" -f ~/.ssh/school

# Output success message
echo "RSA key pair generated successfully."

